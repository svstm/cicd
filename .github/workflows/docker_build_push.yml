name: CI/CD Pipeline

on: push

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build Docker image
      run: docker build -t myimage:${{ github.sha }} .

    - name: Run unittests
      run: docker run myimage:${{ github.sha }} python -m pytest .

    - name: Push Docker image
      if: ${{ success() }}
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: |
          docker tag myimage:${{ github.sha }} myusername/myimage:${{ github.sha }}
          docker push myusername/myimage:${{ github.sha }} run my_image pytest